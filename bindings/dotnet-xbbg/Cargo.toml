[package]
name = "dotnet-xbbg"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "C#/.NET bindings for xbbg Bloomberg engine"
publish = false

# Not yet enabled: this crate is commented out of `[workspace] members` in the
# root Cargo.toml. When it is turned on, add `csbindgen` to
# `[workspace.dependencies]` and inherit every dependency with
# `{ workspace = true }` — do not restate versions here. The previous stale pins
# (arrow 57.1.0, csbindgen 2 — a version that has never been published) are gone.

# [lib]
# crate-type = ["cdylib"]
