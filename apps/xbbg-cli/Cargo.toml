[package]
name = "xbbg-cli"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Command-line interface for Bloomberg data via xbbg"
publish = false

# Not yet enabled: this crate is commented out of `[workspace] members` in the
# root Cargo.toml. When it is turned on, add `clap` to `[workspace.dependencies]`
# and inherit every dependency with `{ workspace = true }` — do not restate
# versions here. Previous stale pins (arrow 57.1.0) are deliberately gone.

# [[bin]]
# name = "xbbg"
# path = "src/main.rs"
